/// Public-facing legal URLs. Required by App Review (Guideline 5.1.1) — the
/// Privacy Policy must be reachable both from inside the app and from the
/// App Store Connect "Privacy Policy URL" field.
///
/// Update these constants when the marketing site publishes the final pages.
class LegalUrls {
  static const String privacyPolicy =
      'https://dowellpestcontrol.com/privacy-policy';
}
