class AnimationTimings {
  static const initialLoadDelay = Duration(milliseconds: 350);

  static const iconIntroDuration = Duration(milliseconds: 550);

  static const nameRevealStartDelay = Duration(milliseconds: 100);
  static const nameRevealDuration = Duration(milliseconds: 450);

  static const mascotFloatStartDelay = Duration(milliseconds: 200);
  static const mascotFloatDuration = Duration(milliseconds: 350);

  static const taglineStartDelay = Duration(milliseconds: 100);
  static const taglineLine1Duration = Duration(milliseconds: 300);
  static const taglineLine2Delay = Duration(milliseconds: 100);
  static const taglineLine2Duration = Duration(milliseconds: 300);

  static const buttonsStartDelay = Duration(milliseconds: 200);
  static const buttonsDuration = Duration(milliseconds: 450);
}
