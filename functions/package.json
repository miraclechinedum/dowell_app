{
  "name": "dowell-functions",
  "private": true,
  "main": "lib/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint --ext .ts src scripts",
    "test": "npm run build && node --test lib/test/unit/role-policy.test.js lib/test/unit/role-claims.test.js lib/test/unit/admin-authorization.test.js",
    "test:rules": "firebase emulators:exec --project demo-dowell --only firestore 'mocha --require ts-node/register test/firestore.rules.test.ts'",
    "test:storage-rules": "firebase emulators:exec --project demo-dowell --only storage 'mocha --require ts-node/register test/storage.rules.test.ts'",
    "test:referrals": "npm run build && firebase emulators:exec --project demo-dowell --only firestore 'node --test lib/test/referrals.integration.test.js'",
    "test:tasks": "npm run build && firebase emulators:exec --project demo-dowell --only firestore 'node --test lib/test/employee-tasks.integration.test.js'",
    "test:account-deletion": "npm run build && firebase emulators:exec --project demo-dowell --only firestore 'node --test lib/test/account-deletion.integration.test.js'"
    ,"test:reconciliation": "npm run build && firebase emulators:exec --project demo-dowell --only firestore 'node --test lib/test/account-reconciliation.integration.test.js'"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "@types/mocha": "^10.0.10",
    "@typescript-eslint/eslint-plugin": "^8.41.0",
    "@typescript-eslint/parser": "^8.41.0",
    "eslint": "^8.57.1",
    "mocha": "^11.7.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
